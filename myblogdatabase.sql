create database myblogdatabase;
use myblogdatabase;

# dane każdego kolejnego użytkownika z rejestracji przez stronę bloga przechowuje tabela regist; 
#pytanie do sprawdzenia dancyh zebranych w tabeli regist
select* from regist;

